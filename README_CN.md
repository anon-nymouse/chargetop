# Chargetop (macOS 终端电池监控器) 🔋

这是一个高性能、单二进制文件的终端用户界面 (TUI) 应用程序，用于监控您的 macOS 电池状态。基于 Go 语言和 Bubbletea 框架构建。

![Battery Monitor Screenshot](https://via.placeholder.com/800x400?text=Battery+Monitor+Go+Screenshot)

## 功能特点

- **极速启动**: 原生二进制文件compiled，启动瞬间完成，内存占用极低。
- **实时监控**: 实时更新电池百分比和充电状态。
- **详细统计**: 循环次数、电池健康状况、最大容量、充电功率 (瓦特) 和序列号。
- **精美界面**: 由 Bubbletea 驱动的现代化终端界面。

## 安装指南

### 方法 1: 源码编译 (需要 Go 环境)

1.  克隆代码仓库:
    ```bash
    git clone https://github.com/aezizhu/chargetop.git
    cd chargetop
    ```

2.  编译二进制文件:
    ```bash
    go build -o chargetop
    ```

3.  运行:
    ```bash
    ./chargetop
    ```

### 方法 2: Homebrew

```bash
brew tap aezizhu/chargetop
brew install chargetop
```

## 使用说明

- **启动**: `./chargetop`
- **退出**: 按 `q` 键 或 `Ctrl+C`
- **刷新**: 按 `r` 键

## 系统要求

- macOS (在 Sonoma/Ventura 上测试通过)
- Go 1.19+ (用于源码编译)
